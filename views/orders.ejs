<!-- this need to be replaced with the actual
orders page, temp page  -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Orders</title>
</head>
<body>
  <h1>User Orders</h1>
  <% if (orders && orders.length > 0) { %> <!-- Check if orders exists and has items -->
    <ul>
      <% orders.forEach(order => { %>
        <li>Order ID: <%= order.id %>, User ID: <%= order.user_id %>, Active: <%= order.active %>, Estimated Time: <%= order.estimated_time_minutes %> minutes</li>
      <% }); %>
    </ul>
  <% } else { %>
    <p>No orders available.</p> <!-- Display a message when there are no orders -->
  <% } %>
</body>
</html>



